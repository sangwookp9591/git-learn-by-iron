# 온보딩 구현·브라우저 검증

2026-09-16, `/Users/iron/orca/projects/gitlearn`. 승인된 `content/onboarding.md`의 S1~S7과 0~6단계를 구현했습니다. 기존 네 콘텐츠 레슨과 `src/engine`, `src/terminal`, 채점기 및 runner는 변경하지 않았습니다. 신규 `onboarding.yaml`은 기존 오후 fixture를 기반으로 main에서 시작하며 기능 커밋과 .gitignore 커밋을 분리합니다. 사용자에게 보이는 완료 표시는 주석이나 명령 문자열 판정이 아니라 실제 Git 상태 채점 이후에만 나옵니다.

랜딩에서 친 `git status`와 실제 출력이 워크벤치에 그대로 이어집니다: PASS. `app.ts`의 화면 전환은 이미 만든 runner와 터미널 DOM을 유지하며 저장소를 다시 만들지 않습니다. 브라우저 완주 스크립트에서 명령·main·.env.local 출력 보존을 검사했습니다.

첫 명령 실행까지 1.657초: PASS, 목표 30초 이내입니다.

첫 성공인 실제 상태 출력까지 1.823초: PASS, 목표 60초 이내입니다.

측정값은 로컬 Chromium의 신규 컨텍스트에서 navigation 시작부터 performance mark까지이며, 스크린샷 대기 600ms를 포함한 자동 조작 1회의 관측입니다. 실제 학습자 읽기 시간이나 모집단 중앙값은 아닙니다. 약 9분이라는 수업 시간은 설계 목표이며 사람을 대상으로 검증하지 않았습니다.

`play-onboarding.js`로 랜딩 Enter → 파일 열람 → diff → Validator의 + 버튼 → 나가기 → 새로고침 → 동일 2단계 복원 → CLI로 두 파일 담기 → staged diff → 소스 컨트롤에서 기능 커밋 → 그래프 커밋 열람 → .gitignore 편집 → 별도 커밋 → 완료를 실제로 수행했습니다. 건너뛰기 없이 7단계를 끝냈고, 새로고침 전후 단계·index·터미널 기록을 비교했습니다. 명령 오타 제안, 나중에 하기, 전체 목록, 다음 레슨 시작도 실행했습니다. 이 실행의 pageerror는 0건입니다.

`check-safety.js`는 다른 컨텍스트에서 CTA 시작, 초급의 실제 21초 무입력, 모든 힌트 이후 정답 노출, 정답 직후 대신 실행 미노출, 재실패 후 대신 실행, 2·4·6단계의 실제 대행을 검증했습니다. 잘못된 fix 커밋은 단계 시작점으로 mixed reset하고 기능만 다시 담아 복구했으며, 사용자가 .gitignore에 추가한 .cache/ 규칙을 보존한 채 .env.local을 덧붙였습니다. 완료 화면에 대신 실행한 단계를 기록했습니다. 중급은 21초 대기만으로 힌트가 나오지 않고 오류 뒤에 나왔으며, 고급은 오류 뒤에도 요청 전에는 나오지 않았습니다. 중급·고급에는 정답 버튼이 없었습니다. localStorage getter가 SecurityError를 던지는 컨텍스트에서도 시작·실습이 동작하고 저장 실패 문구가 표시됐습니다. 계정 폼 제출은 성공을 가장하지 않았습니다.

최종 `npm test`는 기존 29건과 신규 2건을 포함한 31건 통과, 실패·skip 0건입니다. `npm run build`와 `git diff --check`도 통과했습니다. 원문 로그는 [test-output.txt](test-output.txt), [build-output.txt](build-output.txt)에 있습니다. 새 상태 회귀는 7단계 완주, 잘못 담긴 .env.local 및 잘못된 커밋 타입 거부, 저장소 재개, 두 커밋 분리와 저장 실패를 검사합니다.

S1 증거: [랜딩 라이트](S1-light.png), [390px 라이트](S1-390-light.png), [390px 다크](S1-390-dark.png). S2 증거: [동일 터미널을 유지한 첫 진입](S2-entry-light.png). S3 증거: [스테이징 후 진행](S3-staged-light.png), [390px 다크](S3-workbench-390-dark.png), [390px 라이트](S3-workbench-390-light.png). S4 증거: [완료 라이트](S4-complete-light.png), [완료 다크](S4-complete-dark.png), [대신 실행 기록](S4-assisted-390-dark.png). S5 증거: [다섯 묶음 목록](S5-catalog-dark.png), [390px 목록](S5-catalog-390.png). S6 증거: [이어하기](S6-returning-light.png). S7 증거: [워크벤치 안의 힌트·정답](S7-help-light.png). 모두 이 보고서와 같은 디렉터리이며 실제 렌더링된 스크린샷을 열어 확인했습니다. 390px에서 가로 넘침이 없고 랜딩 입력창은 첫 뷰포트 안에 있습니다. 라이트에서도 터미널은 기존 어두운 토큰을 유지합니다.

구현 판단은 다음과 같습니다. 관찰 단계 1·3·5는 읽었음을 확인하는 버튼을 사용합니다. 채점이 상태만 보아야 하므로 사용자가 정확히 어떤 명령을 쳤거나 GUI를 썼는지는 통과 조건으로 강제하지 않았습니다. 브라우저 검증에서는 + 버튼과 git add를 모두 직접 사용했습니다. 대신 실행의 ‘정답 이후에도 진행 없음’은 재실패 또는 20초 경과로 구체화했습니다. 파일 편집은 6단계, 커밋 폼은 4단계에 노출해 작은 데스크톱에서도 터미널과 파일 목록이 함께 보이도록 했습니다. 파일·커밋 변화 애니메이션은 기존 duration 토큰의 3배이며 reduced-motion을 따릅니다.

미구현·제약을 명시합니다. 실제 계정 생성·인증·서버 진도·기기 간 동기화는 백엔드가 없어 구현하지 않았습니다. 이메일은 저장·전송하지 않고 연결 불가를 안내합니다. 세 레슨 완료 후 미완료 레슨에 계정 연결 안내를 띄우는 분기는 구현했으나, 실제 세 레슨 연속 완주 후의 제한 해제는 서버와 함께 별도 검증해야 합니다. 다섯 묶음은 있지만 현재 작성된 실행 레슨 여섯 개만 제공하며 50개 전체와 비어 있는 묶음의 새 레슨은 만들지 않았습니다. 전환율·이탈률·힌트 자력 통과율 등 모집단 지표는 미측정입니다. 복습은 구현된 선행 레슨 사이에서 10일·힌트 2회/대행 조건을 적용하지만, 10일·30일 경과 화면은 실제 장기 방문으로 검증하지 않았습니다. 여러 탭 동시 편집의 충돌 처리는 제공하지 않습니다. 서버 연동 지점과 저장 범위는 README에도 기록했습니다.
